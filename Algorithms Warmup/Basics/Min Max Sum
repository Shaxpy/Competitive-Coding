#include <bits/stdc++.h>
#include <vector>
#include<iostream>
using namespace std;

int main()
{
//    int m=0;n=1;
    long long a[50];long long r=0;
    int nresult=0,mresult=0;

    for (int i=0;i<5;i++)
    {
        cin>>a[i];
        r+=a[i]; 
        
     }
 std::sort(a, a+5);
    cout << r - a[4] << " " << r - a[0] << endl;



return 0;
}
