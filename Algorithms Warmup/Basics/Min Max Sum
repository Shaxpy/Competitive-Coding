#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main()
{

    long long a[5];int r=0;
    int nresult=0,mresult=0;

    for (int i=0;i<5;i++)
    {
        cin>>a[i];
        r+=a[i]; 
        
     }
 sort(a, a+5);//Sorting Elements
    cout << r - a[4] << " " << r - a[0] << endl;


return 0;
